# Final output

Printed by the final step once the PR/MR is open — two lines:

```text
PR/MR: <pr_mr_url>
Jira:  <site>/browse/<TICKET>
```

`<site>` is your Jira site host. The reviewer is requested on the PR/MR itself; nothing is written back to the Jira
issue.
